package mappers;

import domain.Transaction;
import domain.Transactor;
import domain.internal.DC;

import java.util.List;

public class TransactorController {

    public static Transactor getTransactor(int id){

        return new DC();
    };
}
